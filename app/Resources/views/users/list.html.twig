{% extends 'base.html.twig' %}

{% block body %}
<h1>Users:</h1>
<ul>
    {% for user in users %}
        <li><a href="{{ app.request.baseUrl }}/{{ user.screen_name }}">{{ user.name }}</a></li>
    {% endfor %}
</ul>
{% endblock %}
